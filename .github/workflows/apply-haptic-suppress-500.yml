name: Apply haptic suppress 500ms

on:
  push:
    branches: [ dev ]
    paths:
      - '.github/workflows/apply-haptic-suppress-500.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0
      - name: Apply change
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          main = Path('native/src/main.cpp')
          text = main.read_text()
          old = 'constexpr int64_t kHapticConfirmSuppressMs = 120;'
          new = 'constexpr int64_t kHapticConfirmSuppressMs = 500;'
          if old not in text:
              raise SystemExit('expected 120ms haptic suppress constant not found')
          main.write_text(text.replace(old, new, 1))

          build = Path('app/build.gradle.kts')
          gradle = build.read_text()
          if 'versionCode = 44' not in gradle or 'versionName = "0.8.1-dev.2"' not in gradle:
              raise SystemExit('expected dev.2 version not found')
          gradle = gradle.replace('versionCode = 44', 'versionCode = 45', 1)
          gradle = gradle.replace('versionName = "0.8.1-dev.2"', 'versionName = "0.8.1-dev.3"', 1)
          build.write_text(gradle)
          PY
      - name: Commit changes
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add native/src/main.cpp app/build.gradle.kts
          git commit -m "tune: extend haptic confirm dedup to 500ms"
          git push origin HEAD:dev
